services:
  - type: web
    name: whitelisthub
    env: node
    buildCommand: npm ci && npm run build:dashboard
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /api/health
